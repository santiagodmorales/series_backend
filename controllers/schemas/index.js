const userSchema = require('./userSchema');
const serieSchema = require('./serieSchema');
const chapterSchema = require('./chapterSchema');
module.exports = {
    userSchema,
    serieSchema,
    chapterSchema,
}